This directory is intentionally kept empty (and is git-ignored for outputs).

Per-system analyses should live in each system subdirectory, e.g.:
- dimer/analysis/
- dimer_ksat/analysis/
- octahedron/analysis/
- ksat/analysis/

Use the shared scripts in the `state_change/` root and the task runner under `benchmark/`.
